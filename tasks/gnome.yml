---
- name: add numix repo
  apt_repository:
    repo: ppa:numix/ppa
  become: true
- name: install numix theme and icons
  apt:
    name: 
      - numix-gtk-theme
      - numix-icon-theme
  become: true
- name: apply gsettings
  command: "{{ home }}/.yadm/dconf/restore"
